# Dual-Branch Feature Fusion and Multi-Region Ensemble for Ultra-Fine-Grained Plant Leaf Visual Recognition

This repository contains the official implementation of the paper:

> *Dual-Branch Feature Fusion and Multi-Region Ensemble for Ultra-Fine-Grained Plant Leaf Visual Recognition**
> Submitted to *The Visual Computer* (2026).

[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.20046011.svg)](https://doi.org/10.5281/zenodo.20046011)

## 🔗 Permanent Resources

- **Source Code**: [GitHub Repository](https://github.com/mygithubcount1/DCMR_LRNet)
- **DOI**: [10.5281/zenodo.20046011](https://doi.org/10.5281/zenodo.20046011)

## Environment

There are two independent runtime environments for this project:

### 1. Main Training & Inference Environment

For model training, validation and inference

- Python == 3.8.20
- torch == 1.13.0
- timm == 1.0.15
- numpy == 1.21.6
- pandas == 1.1.5
- Pillow == 9.3.0

###  2. Special Environment for `texture_extractor.py`

Only for texture feature extraction

- Python == 3.6.13
- opencv-python == 4.3.0
- scikit-image == 0.17.2
- Pillow == 8.4.0
- numpy == 1.19.2

## Dataset Availability

You can download the datasets from the links below:

+ [SoyLocal, SoyGlobal, SoyGene, SoyAge, and Cotton](https://pan.baidu.com/s/1bPJYmFGWJg2eTr5Ipfw6uA). Access code: iccv
+ [SoyCultivar200](https://drive.google.com/file/d/1XsWZPYYrDsCwAy5r4t3I1F_lOOrGGhgf/view)

## Project Structure
```
DCMR_LRNet/
├── 📁 dataset/                     # Dataset files (images & annotations)
│   ├── 📁 COTTON/                   # Cotton dataset
│   │   ├── 📁 anno/                 # Annotation files
│   │   ├── 📁 images/               # Raw images
│   │   └── 📁 images_pytorch/       # Processed texture images generated by texture_extractor.py
│   └── 📁 soybean200/               # Soybean dataset
│       ├── 📁 anno/                 # Annotation files
│       ├── 📁 images/               # Raw images
│       └── 📁 images_pytorch/       # Processed texture images generated by texture_extractor.py
├── 📁 logs/
│   └── 📄 evaluate.log              # Evaluation log file
├── 📁 weights/                      # Pre-trained backbone weights
│   └── 📄 efficientnet_b0_ra-3dd342df.pth  # EfficientNet-B0 pre-trained checkpoint
├── 📁 weights_COTTON_seed3405/      # COTTON checkpoints (seed 3405)
│   └── 📄 model-max_acc.pth         # Trained model weights with best accuracy
├── 📁 weights_COTTON_seed3407/      # COTTON checkpoints (seed 3407)
│   └── 📄 model-max_acc.pth         # Trained model weights with best accuracy
├── 📁 weights_COTTON_seed3409/      # COTTON checkpoints (seed 3409)
│   └── 📄 model-max_acc.pth         # Trained model weights with best accuracy
├── 📁 weights_soybean200_seed3407/  # soybean200 checkpoints (seed 3407)
│   └── 📄 model-max_acc.pth         # Trained model weights with best accuracy
├── 🐍 dataset.py                    # Dataset loading script
├── 🐍 dataset_soycultivar200.py    # Dataset script for soybean200
├── 🐍 DCBA.py                       # Core model/attack script
├── 🐍 DCBA_resnet.py                # ResNet-based DCBA implementation
├── 🐍 evaluate.py                   # Evaluation script
├── 📄 evaluate.sh                  # Shell script for evaluation
├── 🐍 model.py                      # Model definition
├── 🐍 model_resnet.py               # ResNet-based model definition
├── 📄 README.md                    # This file
├── 🐍 texture_extractor.py          # Texture feature extraction script
├── 🐍 texture_extractor_COTTON.py   # Texture extractor for COTTON dataset
├── 🐍 train.py                      # Main training script
├── 📄 train.sh                      # Shell script for training
├── 🐍 train_resnet.py               # ResNet-based training script
└── 🐍 utils.py                      # Helper functions
```

## Run the experiments

**Important Note**: Please run `texture_extractor.py` to generate processed texture images in `dataset/*/images_pytorch/` **before conducting experiments.**

You can train the model using the corresponding training scripts.

For training with **EfficientNet-B0** (e.g., on the COTTON dataset):

```python
python train.py  --dataset COTTON --seed 3407
```

For training with **ResNet-50** (e.g., on the COTTON dataset):

```python
python train_resnet.py  --dataset COTTON --seed 3407
```

We provide a `train.sh` script that contains all the detailed training commands for quick reproduction.

## Evaluate

We provide an evaluation script for the EfficientNet-B0 baseline. You can reproduce the results by downloading our preprocessed datasets and trained model weights, placing them according to the project structure described above, and running the evaluation commands.

To evaluate the model, use `evaluate.py`. For example, to evaluate the model trained on the Cotton dataset with seed 3407:

```
python evaluate.py --dataset COTTON --seed 3407 --trained_weights ./weights_COTTON_seed3407/model-max_acc.pth
```

We also provide an `evaluate.sh` script that includes ready-to-use evaluation commands for all settings.

The download links for our preprocessed datasets and trained model weights are as follows:

### 📦 Download Resources
| Resource | Description | Link |
|----------|-------------|------|
| Preprocessed Datasets | COTTON & soybean200 | [Baidu Netdisk](https://pan.baidu.com/s/1gDTzdONoZeOYln802qSSZA?pwd=hn28) (Access code: `hn28`) |
| Model Weights | Trained model weights (checkpoints for COTTON & soybean200) | [Baidu Netdisk](https://pan.baidu.com/s/1gDTzdONoZeOYln802qSSZA?pwd=hn28) (Access code: `hn28`) |

## Key Parameters

| Parameter     | Recommended Value                                            | Description                           |
| ------------- | ------------------------------------------------------------ | ------------------------------------- |
| Optimizer     | SGD                                                          | Stochastic gradient descent optimizer |
| Weight Decay  | $1 \times 10^{-4}$                                           | L2 regularization coefficient         |
| Batch Size    | 4( EfficientNet-B0),8(ResNet-50)                             | Training batch size                   |
| Epochs        | 160                                                          | Total training epochs                 |
| Learning Rate | Cosine annealing scheduling;initial lr = $1 \times 10^{-3}$, final lr = $1 \times 10^{-5}$ | Learning rate adjustment strategy     |

## 📖 Citation 

**This code repository is directly related to the manuscript currently submitted to The Visual Computer.**

If you use this code in your research, please cite our submitted paper:

```
@article{Li2026DualBranch,
  title={Dual-Branch Feature Fusion and Multi-Region Ensemble for Ultra-Fine-Grained Plant Leaf Visual Recognition},
  author={Li, Mingbo and Li, Jinyang and Chang, Xue and Chen, Li-an and Wu, Yongfei and Zhou, Daoxiang and Feng, Shu},
  journal={The Visual Computer},
  year={2026},
  volume={},
  number={},
  pages={},
  note={Submitted},
  url={https://github.com/mygithubcount1/DCMR_LRNet},
  doi={10.5281/zenodo.20046011}
}
```

 ## 🔗 Related Information

This repository is directly associated with the manuscript submitted to The Visual Computer.